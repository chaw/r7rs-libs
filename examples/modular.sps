(import (scheme base)
        (scheme write)
        (slib math-integer)
        (slib modular)
        )

(display (extended-euclid 23 57)) (newline)

(display (quotient 3 2)) (newline)
(display (quotient-ei 3 2)) (newline)
